function transform (root, prev) {
    var hits = root.hits;
    var results = [];
    hits.forEach(function(prop, index){
        var result =
            results.push(result);
    });

    return root.hits;
}


// var hits = root.hits;
// return Object.keys(hits).reduce(function(tot, node){
//     return root[node];
// })